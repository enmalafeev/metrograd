# Метроград

Демо мобильного 3D-симулятора поезда метро в стиле пиксель-арт. Движок — **Godot 4.7**.

Управляем поездом из кабины машиниста: разгон и торможение рычагом контроллера,
остановка у платформы, открытие/закрытие дверей. Двухпутная линия из шести станций
(Восточная → Центральная → Заречная → Парковая → Северная → Западная) с боковыми
платформами у каждого пути. На конечных — настоящий разворот: поезд по петле
съезжает на встречный путь и едет обратно по нему (замкнутый маршрут-кольцо).

## Запуск на Mac

```
godot --path .
```
(Godot ставится через `brew install --cask godot`.)

## Управление

- **Рычаг справа** (тач/мышь): вверх — тяга, вниз — тормоз, отпустить — выбег (нейтраль).
- **Кнопка «ДВЕРИ»**: открыть/закрыть двери (доступна только при точной остановке у платформы).
- На компьютере дублируется клавишами: **W** — тяга, **S** — тормоз, **пробел** — двери.

Цель: плавно затормозить так, чтобы кабина оказалась у центра платформы,
открыть двери, подождать посадку, закрыть двери и ехать дальше. На конечной поезд
проходит разворотную петлю, выезжает на встречный путь и едет обратно по линии.

## Структура

- `project.godot` — настройки (landscape, фильтрация Nearest для пиксель-арта).
- `scenes/main.tscn` — корень: 3D-мир в `SubViewport` (640×360) выводится на экран
  через `TextureRect` с фильтрацией Nearest → чёткая «пиксельная» картинка.
- `scripts/world.gd` — процедурная сборка двухпутного тоннеля, станций и разворотных
  петель; строит замкнутую кривую маршрута (`Curve3D`) и список остановок.
- `scripts/train.gd` — движение по кривой маршрута (кинематика тяги/торможения/инерции)
  + кабина и камера; направление задаёт геометрия пути (петли), а не сам поезд.
- `scripts/main.gd` — HUD, управление, логика маршрута и остановок.
- `scripts/cab_controls.gd` — рычаг контроллера машиниста.
- `assets/textures/` — пиксельные текстуры (генерируются `tools/gen_textures.py`).

## Пересобрать текстуры

```
python3 tools/gen_textures.py
```

## Дальше (вне демо)

Экспорт APK на Android, звук кабины/тоннеля, приборы, повороты пути, пассажиры и расписание.
